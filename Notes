*** Npm install
    3 vulnerabilities
    nsp, open, localtunnel
    run "npm audit fix --force"
    npm security check: "nsp check"

*** npm start = npm run start
    npm test = npm run test


*** use inline-source-map
    add 'bugger;' to javascript code and the browser will pause while running into debugger


*** eslintrc.json
    Rules: no-console (0: Off, 1: Warning, 2: Error)

*** remove nsk check while starting the app
    need to resolve all vulnerabilities after
